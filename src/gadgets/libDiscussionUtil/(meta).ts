import type { GadgetMeta } from '@/tools/gadget/types'

export default {
	pages: [{ type: 'existing', name: 'libDiscussionUtil.js' }],
	withResourceLoader: true,
	defaultEnabled: false,
	hidden: true,
	type: 'general',
} satisfies GadgetMeta
